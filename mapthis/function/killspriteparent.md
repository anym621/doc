# killSpriteParent

`killSpriteParent(父图块：Node，图层名：String)`



** 描述：**

 每次图块父节点从舞台上清除时调用。



 **示例：**
```javascript
this.mapjs=this.MapNode.getComponent(this.MapStr);        
this.mapjs.killSpriteParent=function(n,LayerName){              
     //要处理的内容                                                        
}    
```
